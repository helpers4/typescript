{
  "label": "Type",
  "smallDescription": "Compile-time TypeScript utility types",
  "description": "Pure compile-time TypeScript utility types: `DeepPartial`, `DeepWritable`, `Maybe`, `Brand`, `Prettify`, `Nullable`, `ValueOf`, and structural helpers (`DeepGet`, `DeepSet`, `UnionToIntersection`, …). No runtime footprint — import with `import type`."
}
